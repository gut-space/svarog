CREATE TABLE version (
    id serial primary key,
    "version" int not null
);

INSERT INTO "version" ("version") VALUES (5);

-- Compatibility with Celestrak
UPDATE satellites
SET sat_name=REPLACE(sat_name, '-', ' ')
WHERE sat_name LIKE 'NOAA-%';