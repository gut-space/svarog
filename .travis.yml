language: python

python:
  - "3.6"
  - "3.7"
  - "3.8"

before_install:
  - pip3 install pytest
  - pip3 install DateTimeRange
  - pip3 install orbit_predictor

script:
  - cd automation && pytest -v
  - cd ../backend && PYTHONTEST=. pytest -v

notifications:
  email:
    recipients:
      - spam.travis@klub.com.pl
    on_success: change
    on_failure: always
